[project]
name = "bifrost-418-precedence-repro"
version = "0.0.0"
requires-python = ">=3.10"

[tool.uv.sources]
metapathology = { path = "../../", editable = true }

[dependency-groups]
dev = ["metapathology"]
